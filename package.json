{
  "name": "expenses-manager",
  "version": "1.0.0",
  "description": "Expenses tracker to be free",
  "main": "index.js",
  "scripts": {
    "start": "cd /client && npm run build && cd .. && env NODE_ENV=PRODUCTION node index.js",
    "dev": "env NODE_ENV=DEVELOPMENT  nodemon --ignore './client' index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ignacio Bejarano",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^4.0.1",
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
